# Docker 构建忽略文件：以下文件/目录不会被复制到镜像中
# 目的：减小镜像体积 + 避免敏感信息（.env）泄露到镜像层

# 环境变量文件（含真实密钥，严禁进入镜像）
.env
.env.*
!.env.example

# Python 缓存与编译文件
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# 测试目录（生产镜像不需要测试代码）
tests/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# 虚拟环境（本地开发用的 venv 不应进入镜像）
.venv/
venv/
env/

# IDE 配置
.vscode/
.idea/
*.swp
*.swo

# Docker 自身文件
Dockerfile
.dockerignore

# Git
.git/
.gitignore

# 文档（可选保留，生产镜像可去掉减小体积）
*.md
docs/
