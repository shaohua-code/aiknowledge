# =============================================================================
# 智能知识中台前端环境变量示例
# 复制本文件为 .env 并填入真实值
#
# 重要说明：
#   - Vite 构建时通过 import.meta.env.VITE_* 读取环境变量
#   - 仅 VITE_ 前缀的变量会暴露给前端代码（安全机制，避免泄露后端密钥）
#   - 这些变量在构建时（npm run build）被硬编码到产物中，运行时无法修改
#   - 修改后需重新执行 npm run build 才能生效
# =============================================================================

# -----------------------------------------------------------------------------
# API 基础地址
# -----------------------------------------------------------------------------
# 后端 API 的基础路径，前端 axios 实例基于此拼接完整 URL
#
# 开发环境：默认 /api，由 vite.config.ts 中的 server.proxy 转发到 localhost:8000
# 生产环境：保持 /api，由 Nginx 反向代理转发到后端 FastAPI 服务
# 独立部署：若前端与后端不同域名，填完整 URL，如 https://api.example.com
VITE_API_BASE_URL=/api

# -----------------------------------------------------------------------------
# 应用标题
# -----------------------------------------------------------------------------
# 显示在浏览器标签页与页面顶部的应用名称
VITE_APP_TITLE=智能知识中台
